specVersion: "v1"
serviceId: "step02"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "step02-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
